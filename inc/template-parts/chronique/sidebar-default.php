<?php
/**
 * Template Part — Sidebar: Default (Unknown Media Type)
 *
 * Fallback sidebar displayed when the chronique's media type doesn't
 * match any of the specific sidebars (livre, film, série, podcast).
 * This prevents a blank sidebar if a new media type is added without
 * a corresponding sidebar template.
 *
 * Used in: sidebar.php (via get_template_part, as the else/default case)
 *
 * @package turningpages
 */
?>

<div class="book-info">
    <p>Informations non disponibles pour ce média.</p>
</div>